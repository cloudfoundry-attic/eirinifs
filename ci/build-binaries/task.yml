---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: eirini/fsbuilder

inputs:
- name: eirinifs

outputs:
- name: binaries

run:
  path: eirinifs/ci/build-binaries/task.sh

